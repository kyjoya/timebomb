import React from 'react';
import PropTypes from 'prop-types';

const HomePage = props => {
  return (
    <div>
      <h2>Homepage</h2>
    </div>
  );
};

HomePage.propTypes = {
  
};

export default HomePage;