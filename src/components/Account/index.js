import React from 'react';
import PropTypes from 'prop-types';

const Account = props => {
  return (
    <div>
      <h1>Account</h1>
    </div>
  );
};

Account.propTypes = {
  
};

export default Account;